{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Ember webview shell. `remote` is REQUIRED: the app loads the Ember server in the webview, and a remote origin gets NO IPC access without it. allow-ember-commands (permissions/app-commands.toml) is equally required — remote origins are denied even this app's own commands unless a permission names them.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "allow-ember-commands"
  ],
  "remote": {
    "urls": [
      "http://localhost:3000",
      "https://ember.tailf4de41.ts.net"
    ]
  }
}
