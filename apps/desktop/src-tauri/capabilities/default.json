{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal core permissions for the Ember webview shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
