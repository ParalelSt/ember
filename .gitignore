# Secrets — never commit
.env
.env.*
!.env.example

# Node
node_modules/
**/node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
apps/web/dist/
apps/api/dist/
dist/
build/
.vite/

# Next.js build output (regenerable — this was showing up as 10k+ "changes")
.next/
**/.next/
.turbo/
next-env.d.ts
*.tsbuildinfo

# Native-shell (Tauri desktop) build artifacts — left in the worktree as
# leftovers from the native-shell branch; regenerable, thousands of files.
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Python venv + caches
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Local audio cache (large, regenerable)
my_music/

# PocketBase — binary (per-platform) + data (per-user)
pocketbase/pocketbase
pocketbase/pb_data/
pocketbase/pb_public/

# Bug-report logger JSONL output (rotates every 2 days)
logs/

# Internal design docs / brainstorming notes — local only
docs/superpowers/
CHANGES.md

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Misc
.cache/
coverage/
.nyc_output/
cookies.txt

# Local roadmap / status notes — not committed
ROADMAP.md

# Local project rules / app reference / agent docs — not committed
/RULES.md
/EMBER.md
/FIXES-TEST-PLAN.md

# Android release signing — NEVER commit (APPS.md explains regeneration)
apps/mobile/android/ember-release.keystore
apps/mobile/android/keystore.properties
